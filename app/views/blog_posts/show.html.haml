- title @blog_post.title

.blog_post
  %h2= @blog_post.title
  = render 'shared/addthis'
  %p.author
    = render_cell :avatar, :show, @blog_post.cached_user
    Posted by
    = link_to @blog_post.cached_user.login, user_path(@blog_post.cached_user)
    on
    = l @blog_post.created_at.to_date
  .body= raw @blog_post.body
  = render 'shared/addthis'

  - if Rails.env.production?
    .adsense= render 'shared/google_adsense'

  = render_cell :comment, :show, @blog_post
  = render_cell :comment, :new, @blog_post, @comment, current_user

- content_for :js do
  = javascript_include_tag "http://s7.addthis.com/js/250/addthis_widget.js#username=flyerhzm"
